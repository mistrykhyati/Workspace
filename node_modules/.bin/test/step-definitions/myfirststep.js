let {defineSupportCode} = require('cucumber');
//import { ReportAggregator, HtmlReporter} from '@rpii/wdio-html-reporter' ;
//const { ReportAggregator, HtmlReporter} =require( '@rpii/wdio-html-reporter');
defineSupportCode(function({Given, When, Then}) {
    Given('I navigate to Economist', () => {
      browser.url('http://www.economist.com');
	  browser.pause(5000);
    });

    //Then('I expect the title of the page "([^"]*)"$/', (title) => {
		Then('I click Subscribe', () => {
			 const myButton = $('=Subscribe');			 
			 myButton.click();
		//expect(browser.getTitle()).to.be.eql(title);
		//console.log("test");
    //  browser.setValue('.gLFyf', text);
     // browser.pause(5000);
	  
    });
});